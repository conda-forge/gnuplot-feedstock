{% set pkgname = "gnuplot" %}
{% set version = "5.2.2" %}
{% set sha256 = "a416d22f02bdf3873ef82c5eb7f8e94146795811ef808e12b035ada88ef7b1a1" %}

package:
  name: {{ pkgname }}
  version: {{ version }}

source:
  url: https://sourceforge.net/projects/{{ pkgname }}/files/{{ pkgname }}/{{ version }}/{{ pkgname }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}
  patches:
    - patches/term_include.patch

build:
  number: 1
  skip: True  # [win]

requirements:
  build:
    - toolchain3
    - pkgconfig
    - ncurses 5.9
    - readline 7.0
    - libgd
    - cairo 1.14.*
    - pango 1.40.*
    - qt 5.6.*
    - freetype 2.8.1
    - glib 2.55.*
  run:
    - ncurses 5.9
    - readline 7.0
    - libgd
    - cairo 1.14.*
    - pango 1.40.*
    - qt 5.6.*
    - freetype 2.8.1
    - glib 2.55.*
    - libgcc

test:
  files:
    - test-data.txt
  commands:
    - gnuplot -e "set terminal dumb; set style histogram; p 'test-data.txt'"
    - gnuplot -e "set terminal png"
    - conda inspect linkages -p $PREFIX $PKG_NAME  # [not win]
    - conda inspect objects -p $PREFIX $PKG_NAME   # [osx]

about:
  home: https://github.com/gnuplot/gnuplot
  license: Gnuplot
  license_file: Copyright
  summary: Gnuplot, plotting from command line

extra:
  recipe-maintainers:
    - croth1
